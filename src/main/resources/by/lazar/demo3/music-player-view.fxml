<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.text.Text?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="by.lazar.demo3.MusicPlayerController"
            stylesheets="@music-player.css">
    
    <!-- Top: Header -->
    <top>
        <HBox alignment="CENTER_LEFT" styleClass="header">
            <padding>
                <Insets top="15" bottom="15" left="20" right="20"/>
            </padding>
            <Label text="🎵 Музыкальный Плеер" styleClass="header-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="📁 Открыть файлы" onAction="#openFiles" styleClass="header-button"/>
        </HBox>
    </top>
    
    <!-- Center: Main content area -->
    <center>
        <SplitPane dividerPositions="0.3" styleClass="main-split">
            <!-- Left: Playlist -->
            <VBox styleClass="playlist-panel">
                <Label text="Плейлист" styleClass="section-title"/>
                <ListView fx:id="playlistView" styleClass="playlist-list" VBox.vgrow="ALWAYS"/>
                <HBox spacing="10" styleClass="playlist-controls">
                    <Button text="➕ Добавить" onAction="#addToPlaylist" styleClass="control-button"/>
                    <Button text="➖ Удалить" onAction="#removeFromPlaylist" styleClass="control-button"/>
                </HBox>
            </VBox>
            
            <!-- Right: Now Playing -->
            <VBox styleClass="now-playing-panel" spacing="20">
                <padding>
                    <Insets top="30" bottom="30" left="30" right="30"/>
                </padding>
                
                <!-- Album Art Placeholder -->
                <VBox alignment="CENTER" styleClass="album-art-container">
                    <Label fx:id="albumArtLabel" text="🎵" styleClass="album-art"/>
                    <Label fx:id="songTitleLabel" text="Выберите трек" styleClass="song-title"/>
                    <Label fx:id="artistLabel" text="" styleClass="artist-name"/>
                </VBox>
                
                <!-- Progress Bar -->
                <VBox spacing="5">
                    <HBox alignment="CENTER_LEFT" spacing="10">
                        <Label fx:id="currentTimeLabel" text="0:00" styleClass="time-label"/>
                        <Slider fx:id="progressSlider" HBox.hgrow="ALWAYS" 
                                min="0" max="100" value="0"/>
                        <Label fx:id="totalTimeLabel" text="0:00" styleClass="time-label"/>
                    </HBox>
                </VBox>
                
                <!-- Playback Controls -->
                <HBox alignment="CENTER" spacing="15" styleClass="playback-controls">
                    <Button text="⏮" onAction="#previousTrack" styleClass="control-button-large"/>
                    <Button fx:id="playPauseButton" text="▶" onAction="#playPause" styleClass="play-pause-button"/>
                    <Button text="⏭" onAction="#nextTrack" styleClass="control-button-large"/>
                </HBox>
                
                <!-- Volume Control -->
                <HBox alignment="CENTER" spacing="10" styleClass="volume-control">
                    <Label text="🔊" styleClass="volume-icon"/>
                    <Slider fx:id="volumeSlider" HBox.hgrow="ALWAYS" 
                            min="0" max="100" value="50"/>
                    <Label fx:id="volumeLabel" text="50%" styleClass="volume-label"/>
                </HBox>
            </VBox>
        </SplitPane>
    </center>
    
    <!-- Bottom: Status Bar -->
    <bottom>
        <HBox alignment="CENTER_LEFT" styleClass="status-bar">
            <padding>
                <Insets top="10" bottom="10" left="20" right="20"/>
            </padding>
            <Label fx:id="statusLabel" text="Готов к воспроизведению" styleClass="status-text"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="fileCountLabel" text="Треков: 0" styleClass="status-text"/>
        </HBox>
    </bottom>
</BorderPane>

